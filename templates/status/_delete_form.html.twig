<form method="post" action="{{ path('app_status_delete', {'id': status.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce status?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ status.id) }}">
    <button class="btn">supprimer</button>
</form>
