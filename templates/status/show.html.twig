{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ status.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_status_index') }}">Retour à la liste des status</a>

    <a href="{{ path('app_status_edit', {'id': status.id}) }}"> | Modifier</a>

    {{ include('status/_delete_form.html.twig') }}
{% endblock %}
